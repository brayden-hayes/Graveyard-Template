[package]
name = "Graveyard-Template"
version = "0.1.0"
edition = "2024"
license = "MIT"

[profile.release]
opt-level = "z"
lto = "fat"

# These fields configure default behavior for uploads with `cargo v5`.
[package.metadata.v5]
upload-strategy = "differential"
slot = 1
icon = "cool-x"
compress = true

[dependencies]
nalgebra = "0.35.0"
vexide = { version = "0.8.0", features = ["full", "default-sdk"] }

[features]
default = ["vex"]
vex = []          # running on the brain
host = []         # running on PC for testing / simulation

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html
